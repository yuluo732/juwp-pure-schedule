# Android release 签名配置模板
#
# 用法：把本文件复制为同目录下的 keystore.properties，填入你自己的签名信息。
#   Copy-Item keystore.properties.example keystore.properties
#
# ⚠️ keystore.properties 与 *.jks 都**已被 .gitignore 排除**，不要把真实密钥与密码提交上来。
#
# 没有这个文件会怎样：
#   `./gradlew assembleRelease` 仍然能构建成功，但会用 **debug 签名**（见 app/build.gradle.kts 的
#   signingConfig 回退逻辑）。debug 签名的包不适合对外发布，且换机器后签名会变、无法覆盖升级。
#
# 申请自己的密钥（一条命令）：
#   keytool -genkeypair -v -keystore my-release.jks -alias my-alias \
#     -keyalg RSA -keysize 2048 -validity 10000
#
# ⚠️ 密钥文件请**多处备份**。丢了之后无法为已安装的版本发布覆盖升级包，
#    用户只能卸载重装（本地课表缓存与设置会一起丢失）。

# 密钥库文件路径（相对仓库根目录，或写绝对路径）
storeFile=my-release.jks

# 密钥库密码
storePassword=改成你的密码

# 密钥别名（genkeypair 时用 -alias 指定的那个）
keyAlias=my-alias

# 该别名对应的密码（通常与 storePassword 相同）
keyPassword=改成你的密码
