# 教务系统凭据模板
#
# 用法：把本文件复制为同目录下的 .env，填入你自己的学号密码。
#   Copy-Item tools\.env.example tools\.env
#
# ⚠️ tools/.env 已被 .gitignore 排除，**不要**把它提交到仓库。
#    tools/ 下的抓取脚本会读这里的值；也可以改用环境变量 JW_ACCOUNT / JW_PASSWORD。

JW_ACCOUNT=你的学号
JW_PASSWORD=你的密码
